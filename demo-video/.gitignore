node_modules
dist
.DS_Store
.env

# Rendered output — regenerated by `remotion render`, never committed.
out

# Everything dropped in public/ is large source media (your recording and
# voice-over). Kept out of git regardless of file type or extension case —
# these files are too big for GitHub. Only .gitkeep is tracked.
public/*
!public/.gitkeep
