"""Database layer — SQLAlchemy session + models + repos.

Demo uses SQLite via `DB_URL=sqlite:///./snapcal.db`. The repository
classes hide the storage choice so swapping to Supabase Postgres later
is a one-place change.
"""
